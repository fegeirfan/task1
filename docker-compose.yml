version: "3.9"

services:

  node:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - python
    environment:
      - NODE_ENV=development

  python:
    build: ./python
    ports:
      - "5000:5000"